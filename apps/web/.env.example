# Server-side only: where the Next.js BFF rewrites /api/admin and /api/public.
ADMIN_GATEWAY_URL=http://localhost:8001
PUBLIC_GATEWAY_URL=http://localhost:8000
# Show the development-only seeded-user login (never enable in production).
NEXT_PUBLIC_DEV_LOGIN=false
