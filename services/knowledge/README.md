# knowledge

This directory is a deployment boundary in the target architecture. Shared contracts live in packages; runtime access must use a dedicated workload identity, least-privilege credentials, private networking, and authenticated typed APIs.
